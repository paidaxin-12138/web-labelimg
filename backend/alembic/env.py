"""Alembic migration environment placeholder.

Run migrations after installing dependencies:

    cd backend
    alembic upgrade head

Initial schema is also created automatically on startup via `init_db()`.
"""
